# beyondthescreen.org

Standalone static site draft for Beyond the Screen.

## Local preview

Open `index.html` directly in a browser, or serve the directory with any static file server.

## Deployment

The domain is managed in Cloudflare. Point the hosting provider for this repository at `beyondthescreen.org` and configure Cloudflare DNS to the provider's target.
